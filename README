base layout for Xiange Linux sytem.

Installation:
	make prefix=path install

History
	0.1.0
		root direcoty OK.

	0.2.0
		RC system OK.
		Bash parameters OK.
		System boot OK.

	0.2.1
		add /etc/hosts file.

	loongson64 
		更换来自CLFS的启动脚本，速度更快
		增加了bash自动补全功能的脚本
		增加了fstab中网络文件NFS的注释，修改后即可使用
		profile中增加了新的别名
		网络控制脚本改由/etc/sysconfig/network-devices/下的网卡文件夹控制

	2010年9月14日更新
		修正了安装时出错的问题
		加强了bash自动补全功能的脚本，其补全代码增加了3倍多。支持更多的命令
		加入了lib32和lib64为连接

	
